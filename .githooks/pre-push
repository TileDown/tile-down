#!/usr/bin/env bash
# pre-push hook: run the local verification stack before pushing.
#
# Install (per repo): git config core.hooksPath .githooks

set -e

ROOT=$(git rev-parse --show-toplevel 2>/dev/null || echo .)
cd "$ROOT" || exit 1

scripts/check-local.sh
