---
title: Designing from system defaults
date: 2026-05-30
description: The system theme starts with native typography, focused spacing, and mode-aware color.
---
# Designing from system defaults

The `system` theme keeps the surface quiet. It uses native font stacks, restrained
color, and CSS theme properties that switch automatically for light and dark mode.

That gives a plain content site a polished baseline without making every tile or
page carry its own design decisions.
